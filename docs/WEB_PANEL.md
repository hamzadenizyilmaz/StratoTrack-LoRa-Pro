# Web Panel

The device starts a Wi-Fi access point.

```text
SSID: GPS-TRACKER-T1
PASS: 12345678
URL : http://192.168.4.1
```

API:

```text
GET /api/status
```

The web panel is for diagnostics and field testing. It does not require internet.
